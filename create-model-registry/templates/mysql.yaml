---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: {{ .Release.Name }}-mysql
    app.kubernetes.io/component: {{ .Release.Name }}-mysql
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/name: {{ .Release.Name }}-mysql
    app.kubernetes.io/part-of: {{ .Release.Name }}
    component: {{ .Release.Name }}-mysql
  name: {{ .Release.Name }}-mysql
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-mysql
  namespace: {{ .Release.Namespace }}
spec:
  ports:
    - name: mysql
      nodePort: 0
      port: 3306
      protocol: TCP
      targetPort: 3306
  selector:
    name: {{ .Release.Name }}-mysql
  sessionAffinity: None
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-mysql
  namespace: {{ .Release.Namespace }}
spec:
  strategy:
    type: Recreate
  replicas: 1
  selector:
    matchLabels:
      name: {{ .Release.Name }}-mysql
  template:
    metadata:
      labels:
        name: {{ .Release.Name }}-mysql
    spec:
      volumes:
        - name: {{ .Release.Name }}-mysql
          persistentVolumeClaim:
            claimName: {{ .Release.Name }}-mysql
      containers:
        - envFrom:
          - secretRef:
              name: {{ .Release.Name }}-mysql
          args:
            - --datadir
            - /var/lib/mysql/datadir
            - --default-authentication-plugin=mysql_native_password
          image: {{ .Values.mysqlImage | quote }}
          imagePullPolicy: IfNotPresent
          name: mysql
          ports:
            - containerPort: 3306
              protocol: TCP
          resources:
            requests:
              cpu: "500m"
              memory: 1Gi
            limits:
              cpu: "500m"
              memory: 1Gi
          volumeMounts:
            - name: {{ .Release.Name }}-mysql
              mountPath: /var/lib/mysql
---